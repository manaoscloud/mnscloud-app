# DetailPageComponent

Import `DetailPageComponent` from `src/app/shared/pages/detail-page`.
Follow the Detail and settings pages contract in app.md.
Use the shared component, never copy its internal markup or CSS into a page.
